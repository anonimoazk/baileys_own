import swaggerJsdoc from 'swagger-jsdoc';

const options = {
  definition: {
    openapi: '3.0.0',
    info: {
      title: 'Baileys Multi-Instance API',
      version: '1.0.0',
      description: 'API REST para gerenciar múltiplas instâncias de WhatsApp usando Baileys',
    },
    servers: [
      {
        url: 'http://localhost:3000',
        description: 'Development server',
      },
    ],
    components: {
      securitySchemes: {
        ApiKeyAuth: {
          type: 'apiKey',
          in: 'header',
          name: 'x-api-key',
        },
      },
    },
    security: [{ ApiKeyAuth: [] }],
  },
  apis: ['./src/routes.ts', './src/controllers/*.ts'],
};

export const swaggerSpec = swaggerJsdoc(options );